from pathlib import Path

# ============================================================
# 共享项目路径
# ============================================================

PROJECT_DIR = Path(__file__).resolve().parent
EXTRACT_DIR = PROJECT_DIR / "CANDOR_extracted"
OUTPUT_DIR  = PROJECT_DIR / "outputs"
ZIP_PATH    = PROJECT_DIR / "CANDOR.zip"

# ============================================================
# 采样与长度过滤
# ============================================================

RANDOM_SEED = 42

MIN_WORDS = 8   # 已从 6 改为 8
MAX_WORDS = 35

# ============================================================
# 语料处理
# ============================================================

TRANSCRIPT_FILENAME      = "transcript_cliffhanger.csv"
MAX_PER_CONV_PER_FEATURE = 5

# ============================================================
# 文本质量过滤
# ============================================================

# 去除停用词/功能词后，feature 候选至少保留的实质内容词数
# "Oh thank you. Thank you. You too as well." → 0 个内容词 → 过滤掉
MIN_CONTENT_WORDS = 3

# 同一 feature 组内，前 N 词相同的句子视为近重复，只保留内容词最多的一条
PREFIX_DEDUP_WORDS = 6

# ============================================================
# 目标语言学特征
# ============================================================

TARGET_FEATURES = [
    "hedge",
    "tag_question",
    "politeness_softened_disagreement",
]

# ============================================================
# 性别标签
# ============================================================

GENDERS = ["female", "male"]
